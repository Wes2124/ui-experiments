"use client"

// Simple theme resolver for chart components
export const resolvedTheme = "dark"

// You can extend this to use next-themes or other theme providers
// For now, we'll default to dark theme to match the trading dashboard aesthetic
